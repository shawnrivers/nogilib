import * as React from 'react';
import { PageContent } from 'client/components/templates/Page';
import { Typography } from 'client/components/atoms/Typography';

export default () => (
  <PageContent
    header={<Typography variant="h1">Staring</Typography>}
  ></PageContent>
);
