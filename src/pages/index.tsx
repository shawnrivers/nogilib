import * as React from 'react';
import { Header } from 'client/components/templates/Page';
import { Typography } from 'client/components/atoms/Typography';

export default () => (
  <React.Fragment>
    <Header>
      <Typography variant="h1">Staring</Typography>
    </Header>
  </React.Fragment>
);
